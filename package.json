{
  "name": "flightlegservice",
  "version": "1.0.0",
  "description": "Azure function app that performs CRUD operations on flightLeg object",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest",
    "prettier:fix": "prettier --write 'src/**/*.{js,ts,tsx}'",
    "prettier:check": "prettier --check  'src/**/*.{js,ts,tsx}'"
  },
  "dependencies": {
    "@azure/cosmos": "^3.15.1",
    "@types/joi": "^17.2.3",
    "http-status-codes": "^2.2.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "@types/node": "^17.0.21",
    "typescript": "^4.0.0",
    "@types/jest": "^26.0.22",
    "npm-run-all": "^4.1.5",
    "prettier": "2.5.1",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4"
  }
}
